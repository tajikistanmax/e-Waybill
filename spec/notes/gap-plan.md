# e-Waybill — план оставшихся гэпов (сверка покрытия по коду)

> Дата: 2026-09-12. Источники сверки: `spec/notes/legacy-full-inventory.md` (боевая MinTransRT),
> `spec/notes/our-platform-inventory.md` (наша) + **прямая проверка кода** (не по докам).
> Ключевые проверенные файлы:
> - Типы ПЛ: `apps/backend/waybill-service/.../domain/WaybillType.java`,
>   `.../print/WaybillPrintService.java` (`templateFor`), `templates/print/*.html`,
>   `packages/shared/lib/{api.ts,i18n.tsx}` (`TYPE_LABELS`, `wb.desc.*`).
> - Борхаты/накладные: `templates/print/waybill2b-attachment.html`, `cmr.html`,
>   `web/WaybillController.java` (`/{id}/consignment`, `ConsignmentRequest`), `web/PrintController.java`.
> - Отчёты: `calc/report/ReportType.java`, `web/ReportController.java` (21 эндпоинт).
> - Справочники: `master-data/domain/*`, миграции `V2/V7/V28/V54/V55/V56/V58`,
>   `waybill-service/domain/MalumotnomaRoute.java`.
> - Роли: `packages/shared/lib/roles.ts`, `infra/keycloak/epd-realm.json`.
>
> Легенда вердикта: **покрыто** / **частично** / **отсутствует**. «(уточнить)» — требует продуктового решения/данных.

---

## 1. Формы / типы путевых листов

Наш `WaybillType` (10 значений) + печатные шаблоны + i18n проверены построчно. Маппинг «боевая форма → наш тип/шаблон»:

| Боевая форма (legacy CRUD) | Наш тип (`WaybillType`) | Печатный шаблон | Вердикт | Комментарий |
|---|---|---|---|---|
| 1-АД Автобус (`waybill1ad` bus) | `WB_BUS` (legacyForm «Т(1-АД)») | `print/waybill1ad` | **покрыто** | |
| 1-АД Троллейбус (`waybill1ad` ebus / `waybill1adeBus`) | `WB_TROLLEYBUS` | `print/waybill1ad` | **покрыто** | отдельный тип, тот же бланк |
| 1-АД для топл. сотрудника (`waybill1adfe`) | — (нет отдельного типа ПЛ) | — | **покрыто иначе** | выдача топлива вынесена в кабинет `FUEL_STATION` (`FuelStationController`), а не в отдельную форму ПЛ. Функционально закрыто. |
| 1-А Микроавтобус (`waybill1a`) | `WB_MINIBUS` (legacyForm «1-А») | `print/waybill1a` | **покрыто** | |
| 3-С Сабукрав (`waybill3c`) | `WB_CAR` + `WB_TAXI` (legacyForm «3-С», «3-С такси») | `print/waybill3c` | **покрыто** | легковой и такси разведены на 2 типа |
| 3-С «30» Душанбе (`waybill3c30`) | — (тот же `WB_CAR`/`WB_TAXI`) | `print/waybill3c` | **покрыто иначе** | 3c30 в боевом = обход для норматива Душанбе (4 ПЛ/сутки). У нас это норматив (`waybill-norm`/policy), отдельная форма не нужна. |
| 2-Б грузовой (`waybill2b`) | `WB_TRUCK` (legacyForm «2-Б») | `print/waybill2b` | **покрыто** | |
| **4-МБМ (`waybill4mbm`)** | **`WB_PAX_INTL`** (legacyForm «4М-БМ», код 08) | `print/waybill1ad` (переиспользуется) | **частично** | Тип заведён (пассажирский международный), печатается через бланк 1-АД. **Своего бланка 4-МБМ нет** — проверить, отличается ли утверждённая форма 4-МБМ от 1-АД; если да — нужен отдельный шаблон. |
| 5Б-БМ грузовой межд. (`waybill5bbm`) | `WB_TRUCK_INTL` (legacyForm «5Б-БМ», код 07) | `print/waybill5bbm` | **покрыто** | |
| **СМР межд. (`cmr` / Cargo5bbm)** | документ на `WB_TRUCK_INTL` | `print/cmr` (`GET /{id}/print-cmr.pdf`) | **покрыто** | СМР реализован как международная накладная к 5Б-БМ (отдельный PDF-эндпоинт), не как отдельный тип ПЛ. |
| **Борхати замимаи 1 (`cargowaybill1attachment`, корбайъ/сдельно)** | данные на `WB_TRUCK`/`WB_DANGEROUS` | `print/waybill2b-attachment` (`shipmentKind != HOURLY`) | **покрыто (форма) / частично (кабинет)** | Один шаблон покрывает **обе** замимы (см. коммент в шаблоне: перенос `bill2b_attachment1.blade.php`). Данные вводит диспетчер через `POST /{id}/consignment`. **Нет отдельного кабинета грузоотправителя** (см. §4). |
| **Борхати замимаи 2 (`cargowaybill2attachment`, соатбайъ/повременно, +экспедитор)** | данные на `WB_TRUCK`/`WB_DANGEROUS` | `print/waybill2b-attachment` (`shipmentKind == HOURLY`, печатается строка «Экспедитор») | **покрыто (форма) / частично (кабинет)** | Строка экспедитора (`forwarderName`) есть. Данные экспедитора вводит диспетчер, не сам экспедитор. |
| Маълумотнома (`malumotnoma`) | справка (waybill-service) | `print/malumotnoma` + QR | **покрыто** | `MalumotnomaController`, `MalumotnomaRoute` (тарифы). |
| — (нет в боевом) | `WB_SPECIAL` (спецтехника, код 09) | `print/waybill` (универсальный) | **сверх эталона** | связан с классификатором `WORK_TYPE`. |
| — (в боевом = режим 2-Б) | `WB_DANGEROUS` (код 10, из выбора убран) | `print/waybill2b` | **сверх/эквивалент** | опасный груз у нас = режим 2-Б (ADR-класс), как и в боевом. |

**Итог по формам:** ключевые формы закрыты. **4-МБМ** — тип есть, но печатается через 1-АД (частично). **Борхаты 1 и 2** — форма/печать/данные покрыты полностью; не хватает только отдельных кабинетов грузоотправителя/экспедитора. **СМР** — покрыто.

---

## 2. Справочники (сверка ~28 боевых с нашими)

| Боевой справочник | Наш аналог | Вердикт | Комментарий / рекомендация |
|---|---|---|---|
| `regions` (Минтақа, CRUD) | `regions` 1..7 (хардкод/enum, `regionId` в сущностях) | **частично** | Регионы РТ статичны — CRUD не критичен. При желании — сид-справочник (S). |
| `cities` (Шаҳру ноҳия) | `City` (V54, 69 записей, `CityController`) | **покрыто** | было ❌ в 04 — устарело. |
| `companies` | `Organization` (fleet/company) | **покрыто** | |
| `parkings` (ТС) | `Vehicle` | **покрыто** | |
| `car` (отд. модель) | — | **н/д** | в боевом почти не используется (техдолг), тащить не надо. |
| `brands` (Тамға) | `Brand` (~488 сидов, V58) | **покрыто** | |
| `drivers` | `Driver` | **покрыто** | |
| `routes` (Хатсайр) | `Route` (+коэф. поля V28) | **покрыто** | маршрут = свободные точки А/Б (структурного маршрута нет — см. группу B). |
| `route_types` (Намуди хатсайр, CRUD) | — (transportType-enum) | **частично** | как справочник отсутствует; для Этапа 1 enum достаточно. (уточнить, нужна ли типизация «город/пригород/межгород»). |
| `directions` (Самт) | `Direction` (~95 сидов, V56) | **покрыто** | было «как classifier» в 04 — устарело. |
| `clients` (Мизоҷ) | `Client` (org-scoped) | **покрыто** | |
| `cargos` (Бор) | `Cargo` (~135 сидов, V55) | **покрыто** | было ❌ в 04 — устарело. |
| `tariffs` (Нархнома) | `Tariff` + `RouteTariff` | **покрыто** | |
| `employees` (врач/механик/…) | `Employee` | **покрыто** | |
| `fuels_table` (виды топлива) | `fuels` 1..5 (хардкод) + `FuelNorm` | **покрыто иначе** | виды топлива статичны — enum достаточно. |
| `fuel_winter_coef` | `FuelWinterCoef` | **покрыто** | |
| `city_coef` | `CityCoef` | **покрыто** | |
| `mountain_coef` | `MountainCoef` | **покрыто** | |
| `used_coef` | `UsedCoef` | **покрыто** | |
| `drive_classes` (Дараҷа) | `DriveClass` | **покрыто** | |
| `external_countries` (Давлатҳо) | `Classifier` категория `COUNTRY` = **14** | **частично** | Боевой ~218 стран, у нас 14 (проверено в `V7__classifiers.sql`). Для реальных межд. рейсов список неполон. |
| `external_cities` (Шаҳрҳои хориҷа, ~19 тыс.) | — (свободный текст в межд. ПЛ) | **отсутствует** | внешний справочник городов не заведён. |
| `malumotnomas` | Malumotnoma (waybill-service) | **покрыто** | |
| `routemalumotnomas` (тариф справки по видам ТС) | `MalumotnomaRoute` (car/mbus/bus price, distance) | **покрыто** | проверено — поля 1:1. |
| `bill_types` (Намуди роҳхат) | `WaybillType` enum + `/settings/types` + available-types/org | **покрыто иначе** | скоуп доступных типов на org — не нужен отдельный редактируемый справочник. |
| `numberdriver` (нумерация) | атомарная нумерация (сервис) | **покрыто иначе** | справочник не нужен. |
| `waybill_plans` (Нақша) | `WaybillPlan` (`WaybillPlanController`) | **покрыто** | |
| `phone_infos` (устройства мобайла) | — (`MobileController` есть, реестра устройств нет) | **отсутствует** | нужен только при мобильном приложении водителя (этап 2). |
| `waybills` (реестр бланков) | — (полностью электронная нумерация) | **н/д** | физический учёт бланков не нужен при 100% электронном ПЛ. |

**Реальные гэпы справочников:** `external_cities` (отсутствует), полнота `COUNTRY` (14 vs 218), `route_types` как справочник (минор), `phone_infos` (этап 2). Остальные боевые справочники закрыты (часть — сознательно через enum/сервис).

---

## 3. Отчёты (сверка боевых типов с нашими)

Наш `ReportType` (11) + 21 эндпоинт `ReportController` проверены по коду.

### 3.1. Мусофирбарӣ / Боркашонӣ (боевые до 14 типов)
| Боевой тип (Мусофирбарӣ) | Наш `ReportType`/эндпоинт | Вердикт |
|---|---|---|
| 1 Автомобил | `BY_VEHICLE` | покрыто |
| 8 Табел | `BY_DRIVER` | покрыто |
| 2 Хатсайр | `BY_ROUTE` | покрыто |
| 3 Тамға | `BY_BRAND` | покрыто |
| 4 Авто | `COMPANY_SUMMARY` | покрыто |
| 5 Музди меҳнати ронандагон | `DRIVER_SALARY` | покрыто |
| 6 Малумот оиди гашт | `TRIP_INFO` | покрыто |
| 7 Дафтари қайди в/н | `REGISTRY_JOURNAL` | покрыто |
| 9 Сузишвори | `FUEL_GENERAL` | покрыто |
| 10 Хисоботи сузишвори | `FUEL_GENERAL` (тот же) | **частично** — вторая презентация топл. свода не выделена (минор). |
| 11 сузишвории ронанда | `FUEL_BY_DRIVER` | покрыто |
| 12 сузишвории автомобил | `FUEL_BY_VEHICLE` | покрыто |
| 13 Дафтари қайди механик | `GET /reports/journal/mechanic` (+.xlsx) | покрыто |
| 14 Дафтари қайди духтӯр | `GET /reports/journal/doctor` (+.xlsx) | покрыто |

Грузовые (2-Б/5Б-БМ) типы — тот же набор разрезов, покрыты теми же `ReportType` через `/cargo`.

### 3.2. Умумӣ (сводный, регион)
Боевой: transportation / _industry / _general, count_waybills(+industry/general), count_waybills_type2, нормативы must_give.
Наш: `/regional`, `/regional-count`, `/waybill-norm` (+.xlsx каждый), `/passenger-volume-trend`. **Вердикт: покрыто** (план/факт, кол-во ПЛ, норматив по типам, тренд).

### 3.3. Маълумотнома
Боевой `ReportMalumotnomaController` → наш таб `/reports/malumotnoma` + `MalumotnomaController`. **покрыто.**

### 3.4. Прогнозы (`config/trans.forecast`: Хатсайр/Минтақа/Шаҳру ноҳия/Корхона)
В боевом **не выведены в UI** (ReportMain закомментирован). У нас нет. **Вердикт: отсутствует, но не блокер** — сам боевой их не эксплуатирует. (уточнить потребность.)

**Итог по отчётам:** покрытие ~полное; XLSX-экспорт — сверх эталона. Реальные мелкие гэпы: раздвоение топл. свода (тип 9/10), прогнозы (не активны и в боевом).

---

## 4. Роли / кабинеты (12 боевых → наши 12)

| Боевая роль | Наш аналог | Вердикт | Комментарий |
|---|---|---|---|
| `superadmin` | `SYSTEM_ADMIN` | покрыто | |
| `admin` | `SYSTEM_ADMIN` (частично `COMPANY_ADMIN`) | покрыто | |
| `company` | `COMPANY_ADMIN` (+`BRANCH_ADMIN` для филиалов) | покрыто | у нас есть уровень филиала, которого в боевом нет. |
| `region` (рег. управление, **пишет** справочники/предприятия региона) | `MINTRANS_ANALYST` (**только чтение**, вся страна) | **частично** | Нет регионально-скоупленного админа с правом ведения. Надзор-чтение закрыт аналитиком. Ведение справочников централизовано у `SYSTEM_ADMIN`. |
| `client_forwarder` (экспедитор — кабинет борхатов) | — | **отсутствует** | борхат-данные вводит диспетчер; отдельного кабинета экспедитора нет. |
| `client_sender` (грузоотправитель — кабинет борхатов) | — | **отсутствует** | то же. |
| `mechanic` | `MECHANIC` | покрыто | |
| `doctor` | `DOCTOR` | покрыто | |
| `fuel_employee` | `FUEL_STATION` | покрыто | |
| `employee_kassa` (кассир) | `ACCOUNTANT` | покрыто иначе | оплата/касса у бухгалтера. |
| `customs_officer` (таможня) | — (поле `customsOfficerName` на СМР-борхате) | **отсутствует** | нет кабинета таможенника; данные таможни вводит диспетчер. |
| `gps` (просмотр GPS-событий) | покрыто функцией мониторинга (`INSPECTOR`/`DISPATCHER`/`ANALYST`/`SYSTEM_ADMIN`) + приём `API_INTEGRATOR` | покрыто иначе | отдельная роль-просмотрщик GPS не нужна. |

**Наши роли сверх боевого:** `INSPECTOR` (дорожный контроль — новое), `DRIVER` (полноценная роль, в боевом был мобильный JWT-доступ), `DISPATCHER` (в боевом — тип сотрудника, не spatie-роль), `MINTRANS_ANALYST`, `BRANCH_ADMIN`.

**Реально отсутствующие роли-кабинеты:** `client_sender` / `client_forwarder` (кабинеты борхатов), `customs_officer`, регионально-пишущий `region`. Нужны ли в Этапе 1 — обосновано ниже.

---

## (A) Реализуемо сейчас — конкретные задачи

| # | Задача | Слой | Объём | Обоснование |
|---|---|---|---|---|
| A1 | **Отдельный печатный бланк 4-МБМ** (`WB_PAX_INTL`): создать `templates/print/waybill4mbm.html` и завести его в `templateFor()` вместо переиспользования `waybill1ad`. | бэк (шаблон) | **S** | Тип уже есть; сейчас печатается чужим бланком. Сначала **уточнить**, отличается ли утверждённая форма 4-МБМ от 1-АД; если нет — задачу закрыть как «покрыто». |
| A2 | **Дополнить справочник стран `COUNTRY`** до полного перечня (сейчас 14). Добавить недостающие в сид/миграцию классификатора. | данные | **S** | Нужно для международных ПЛ/СМР; текущие 14 — только соседи. |
| A3 | **Внешние города (`external_cities`)**: завести справочник-подсказку (по аналогии с `City`) вместо свободного текста в межд. ПЛ и СМР; хотя бы ключевые города стран-партнёров. | данные + бэк CRUD + фронт | **M** | Точность и единообразие межд. накладных. Полный боевой объём (~19 тыс.) грузить не обязательно — начать с приоритетных. |
| A4 | **Разделить топливный свод**: тип 10 (Хисоботи сузишвори) как отдельная презентация рядом с `FUEL_GENERAL` (тип 9), если требуется формой отчётности. | бэк | **S** | Довести отчётность 1:1 к боевой. (уточнить требование заказчика.) |
| A5 | **`route_types` как справочник** (город/пригород/межгород и т.п.), если утверждённые формы требуют явной типизации маршрута. | данные + бэк + фронт | **S/M** | Сейчас enum. Нужность — уточнить. |
| A6 | **Регионы как сид-справочник** (7 записей с кодами) вместо хардкода — задел под региональную отчётность/фильтры. | данные | **S** | Мелко, снимает хардкод. |

> Топ-5 приоритета группы A: **A1 (бланк 4-МБМ), A2 (страны), A3 (внешние города), A4 (топл. свод 9/10), A5 (route_types)**.

---

## (B) Этап-2 / внешне-заблокировано / продуктовое решение

| # | Гэп | Категория | Комментарий |
|---|---|---|---|
| B1 | **Кабинеты грузоотправителя/экспедитора** (`client_sender` / `client_forwarder`) — самостоятельный ввод борхатов замимаи 1/2 контрагентами. | продуктовое + роли | Форма и данные борхатов уже покрыты (вводит диспетчер). Отдельные кабинеты = решение об оргмодели и внешних пользователях. |
| B2 | **Кабинет таможенника** (`customs_officer`) — подтверждение таможни на СМР. | продуктовое + роли | Поле `customsOfficerName` уже есть; кабинет — при интеграции с таможней. |
| B3 | **Регионально-пишущий админ** (`region`): ведение справочников/предприятий в границах региона. | продуктовое + оргструктура | Сейчас надзор = `MINTRANS_ANALYST` (чтение), ведение = `SYSTEM_ADMIN`. Нужен ли децентрализованный региональный админ — решение Минтранса. |
| B4 | **Квалифицированная ЭЦП (УЦ РТ / CAdES)** — активировать `CadesTitleSigner` вместо `StubTitleSigner`. | внешне-заблокировано | единственный оставшийся P0; ждёт подключения УЦ. |
| B5 | **Боевые интеграции** — налоговая/реестры ТС и водителей/SMS/банковский шлюз (сейчас stub `UnifiedPlatformClient`, webhook оплаты без боевого шлюза). | внешне-заблокировано | |
| B6 | **Структурный маршрут** — точки, геозоны, платные/запрещённые участки, пункты пропуска, авторасчёт планового пробега. | продуктовое + данные | сейчас маршрут = свободные точки А/Б. |
| B7 | **Оргструктура перевозчика** — автоколонны, медпункты, пункты ТО, банк-реквизиты как объекты, подразделения. | продуктовое | филиалы есть на уровне скоупа. |
| B8 | **Реестр мобильных устройств** (`phone_infos`) + полноценное мобильное приложение водителя. | этап 2 | `MobileController` (свои ПЛ) есть; учёта устройств нет. |
| B9 | **Прогнозы** (Хатсайр/Минтақа/Шаҳру ноҳия/Корхона). | продуктовое | в боевом не выведены в UI; нужность уточнить. |
| B10 | **Посуточный расчёт топлива многодневных пассажирских ПЛ** — движок (`SequentialFuel`/`MultiDayPassengerCalc`) есть, но не подключён к живому пути. | бэк (не гэп покрытия, а точность) | риск точности 1-А/3-С; вынести в отдельную задачу расчётного ядра. |

---

## Сводка

- **Ключевые формы:** 4-МБМ — тип заведён, но печатается бланком 1-АД (**частично**, A1); **борхаты замимаи 1 и 2 — покрыты** (форма+печать+данные консайнмента), не хватает лишь кабинетов контрагентов (B1); **СМР — покрыто** (`print-cmr`).
- **Справочники:** реальные гэпы — `external_cities`, полнота стран (14/218), `route_types`-справочник (минор), `phone_infos` (этап 2). Прочее закрыто (часть сознательно через enum/сервис) — то, что 04-анализ считал ❌ (`Cargo`/`Direction`/`City`), фактически реализовано.
- **Отчёты:** покрытие ~полное (11 `ReportType` + журналы механика/врача + региональные + XLSX сверх эталона); мелочи — раздвоение топл. свода (9/10), прогнозы (не активны и в боевом).
- **Роли:** отсутствуют кабинеты `client_sender`/`client_forwarder`, `customs_officer`, регионально-пишущий `region`; `gps`/`kassa`/`fuel_employee` покрыты иначе.
