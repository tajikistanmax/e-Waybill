# Чек-лист готовности к продакшену (go-live) — e-Роҳхат

Единый список того, что обязательно закрыть перед реальным запуском. Ведётся живым:
статус обновлять по мере закрытия пунктов. Составлен 2026-09-04.

Легенда статуса: ⛔ блокер · 🟡 частично/риск · ✅ готово · 📋 организационное (вне кода).

---

## 1. Инфраструктура и безопасность контура

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 1.1 | TLS/HTTPS на всём контуре (web/API/QR) | ⛔ | DevOps + госЦОД | Нужен reverse-proxy (nginx/Traefik) с TLS-терминацией. На localhost сознательно не делалось — план в `docker-compose.prod.yml` (TODO) и `05-соответствие-стандартам-АГВ.md` §5.4 |
| 1.2 | Keycloak в прод-режиме (не `start-dev`) | ⛔ | DevOps | Своё Postgres-хранилище вместо dev-store, единый внешний hostname, `KC_HOSTNAME`, отключить dev-фичи |
| 1.3 | Сменить ВСЕ дев-секреты | ⛔ | DevOps | `epd_provisioner_dev_secret`, `epd_aggregator_dev_secret`, `epd_webhook_dev_secret`, пароли Postgres/MinIO/Keycloak-admin, `QR_SIGNING_KEY`, `MEDDATA_ENCRYPTION_KEY`, `BACKUP_ENCRYPTION_KEY` — все в `infra/.env` помечены «в проде заменить» |
| 1.4 | Реальная инфраструктура ЦОД РТ | 📋 | госЦОД | Сейчас — docker-compose на одной машине. Нужны основной+резервный контуры (см. РС-19.1 допущение 1) |
| 1.5 | Rate-limit на общий счётчик (Redis) | 🟡 | Backend | Сейчас in-memory на реплику + весь трафик идёт с одного IP через прокси Next.js — за реальным reverse-proxy с `X-Forwarded-For` это решится, но проверить на целевой топологии |
| 1.6 | Вернуть обязательную 2FA для admin/analyst/inspector | ✅ | — | Возвращена 2026-09-11 (продуктовое решение): `requiredActions: ["CONFIGURE_TOTP"]` проставлен в `epd-realm.json` для admin/analyst/inspector; QA-дубли `*-automation` намеренно без 2FA (регресс-скрипты). Применяется при свежем импорте реалма / на проде; текущий dev-стенд сохраняет демо-состояние до re-import |
| 1.7 | Маршрутизация reverse-proxy на 3 контура фронта | 🟡 | DevOps | Фронт разделён (итерация 8): публичный портал проверки QR — отдельное приложение `apps/public` (мин. поверхность атаки), консоль собирается в профилях `waybill`/`oversight`. Прод-домены: напр. `verify.epd.tj` → public, `lk.epd.tj` → waybill, `nadzor.epd.tj` → oversight; проставить `NEXT_PUBLIC_VERIFY_BASE_URL`/`NEXT_PUBLIC_WAYBILL_URL`/`NEXT_PUBLIC_OVERSIGHT_URL` на боевые домены. Код и сборки готовы (`--profile split`), нужен reverse-proxy с TLS (см. 1.1) |

## 2. Юридическая значимость документа

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 2.1 | Квалифицированная ЭП через нацУЦ РТ | ⛔ | Интеграция + нацУЦ | Сейчас dev-заглушка `StubTitleSigner` (SHA-256). Нужен `epd.signing.mode=cades` + договор с УЦ (Закон № 1965). Отслеживается как РС-19.6 |
| 2.2 | НПА об электронном путевом листе | 📋 | Минтранс/юристы | Пока добровольный режим с печатной формой. Отслеживается как РС-19.1 |
| 2.3 | Электронная печать платформы | 🟡 | Backend | На бланке есть, но завязана на реальную ЭП (2.1) |

## 3. Внешние интеграции (сейчас все stub)

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 3.1 | Единая платформа е-Транспорт (мастер-данные) | ⛔ | Интеграция | `UNIFIED_PLATFORM_MODE=stub`. ИНН/госномера не сверяются реально. До готовности — локальные реестры платформы (РС-19.2) |
| 3.2 | Налоговая (сверка ИНН) | ⛔ | Интеграция | stub — нужен реальный API |
| 3.3 | ГАИ/МВД (сверка госномеров, реестр ВУ) | ⛔ | Интеграция | stub |
| 3.4 | Платёжный шлюз (банк) | 🟡 | Интеграция | Webhook есть (`PaymentWebhookController`), нужен реальный шлюз E-PERMIT (РС-19.8) + сильный `PAYMENT_WEBHOOK_SECRET` |
| 3.5 | SMS-провайдер (доставка номера ПЛ) | ⛔ | Интеграция | Нет провайдера, канал-заглушка |
| 3.6 | Агрегаторы такси (ЧУРА/НЕРУ) | 🟡 | Интеграция | Контракт есть, `AGGREGATOR_OPEN=false` в проде (обязателен client-credentials токен) |

## 4. Данные и миграция

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 4.1 | Очистить тестовый мусор из БД | 🟡 | Backend | Скрипт `scratchpad/cleanup-test-waybills.sql` (392 тестовых ПЛ). Тестовые ТС (SCANTEST/UATACC), юзеры `uat-onboard-*` — тоже удалить. НА ПРОДЕ база должна стартовать чистой |
| 4.2 | Пробная миграция из rohkhat.tj | 📋 | Backend + данные | Сверочные отчёты, контрольные суммы (РС-19.12) |
| 4.3 | Резервное копирование настроено и проверено | 🟡 | DevOps | Скрипты `backup-postgres.ps1`/`restore-postgres.ps1` есть (AES-256+HMAC), нужно расписание + проверка восстановления на проде |
| 4.4 | Ретенция журнала проверок QR (`verify_scan_log`) | 🟡 | Backend + решение | Таблица пишется на каждый анонимный скан verify (§18) и растёт неограниченно (security-аудит L2). Нужна политика хранения (срок + периодическая чистка/партиционирование). Срок хранения — продуктовое/юр. решение (лог обращений к гос-документу) |

## 5. Верификация (главный процессный пробел)

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 5.1 | Независимый пентест | ⛔ | Внешний аудитор | Всё тестирование до сих пор — саморевью (в т.ч. «комиссия 9 ролей» = агенты, настроенные мной). Нужен независимый взгляд |
| 5.2 | Нагрузочное тестирование на целевых цифрах | ⛔ | QA/DevOps | `epd-load-test.js` (k6) есть, но на целевых объёмах массовой обязательности не гонялся (РС-19.9). Баг `/violations` (сотни параллельных запросов) прожил незамеченным именно из-за этого |
| 5.3 | Полный ручной UI-проход всех ролей | ✅ | — | Проведён 2026-09-04, найдено и исправлено: дубль меню водителя, сломанные логины 3 ролей, переключатель филиалов у админа, 429 в `/violations`. Повторять после каждой крупной правки |
| 5.4 | Регрессионный набор зелёный | ✅ | — | smoke 35/35, security-matrix 23/23, blocking-checks 5/5, concurrency 4/4. Перепрогнан 2026-09-11 после разделения фронта (итерация 8) — зелёный. Регрессия при этом поймала реальный NPE в `FuelCalculationService` (тернарник short/Short распаковывал `null`, `/fuel-calculation` → 500) — исправлен явным if/else |
| 5.5 | CI security-job (SAST/SCA/secret-scan) | ✅ | — | gitleaks/Trivy/Semgrep в `.github/workflows/ci.yml` |

## 6. Пилот и организационное

| # | Пункт | Статус | Ответственный | Комментарий |
|---|-------|--------|---------------|-------------|
| 6.1 | Выделены инспекторы ГАИ для пилота | 📋 | ГАИ | РС-19 допущение 6 |
| 6.2 | Обучение/видеоинструкции на тадж. языке | 📋 | Минтранс | РС-19.4 |
| 6.3 | Колл-центр 8808 | 📋 | Минтранс | РС-19.4 |

---

## Сводка блокеров (⛔) — без них запускать нельзя

1. TLS + прод-Keycloak + смена всех дев-секретов (1.1–1.3)
2. Реальная ЭП через нацУЦ (2.1)
3. Реальные интеграции: е-Транспорт, налоговая, ГАИ, SMS (3.1–3.3, 3.5)
4. Независимый пентест + нагрузочное тестирование (5.1–5.2)

**Вывод:** код на текущем стенде функционально работоспособен и регрессия зелёная,
но go-live блокируют преимущественно внешние/организационные пункты (инфраструктура,
УЦ, интеграции, независимая проверка), а не дефекты приложения. Главный процессный
риск — верификация до сих пор велась силами разработки; до реального запуска нужен
независимый аудит.
