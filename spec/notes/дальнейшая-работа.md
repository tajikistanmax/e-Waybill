# Дальнейшая работа — Перенос платформы (бриф владельца, 2026-09-21)

## Бриф (как прислан)

**Цель.** Старая платформа: `D:\Projects\rohkhat.tj ralavel` (PHP/Laravel). Новая: e-Waybill
(Java/Spring Boot + Next.js). Перенести ВСЮ логику 1-в-1: все поля, расчёты, отчёты, проверки.
Ничего не упрощать/пропускать/«улучшать» без явной причины.

**Главное отличие новой версии.** Шаблоны, отчёты и настройки — конфигурируемые: администратор
меняет их через интерфейс/настройки, без кода. Что в старой было «зашито» в код (шаблоны, названия
полей, форматы отчётов) — в новой хранится в настройках/БД.

**Правила работы.**
- Работать автономно, без одобрения на каждом шаге.
- Вся работа — в git-ветке `migration`; коммит после каждого завершённого пункта.
- Не трогать рабочую БД/прод; только тестовая база.
- Ничего не удалять в старом проекте — он только для чтения.

**Порядок.**
1. Изучить старый проект → `MIGRATION.md`: модули, сущности, поля, бизнес-правила, отчёты, шаблоны.
2. По пунктам: (a) перенести логику; (b) тест сравнения новое-vs-старое на одинаковых данных
   (где можно — запускать старую версию как эталон); (c) прогнать тесты, чинить до зелёного;
   (d) отметить `[x]` в MIGRATION.md + как проверено; (e) коммит; следующий.
3. Если 1-в-1 невозможно / логика неясна — не останавливаться: вопрос в «Вопросы к Максу»,
   выбрать самый безопасный вариант, продолжать.
4. В конце — прогнать все тесты и итоговый отчёт.

## Git (блокер снят 21.09)
Системного git нет, но есть portable **MinGit** `%LOCALAPPDATA%\Programs\PortableGit\cmd\git.exe`
(перед push: `$env:GIT_TERMINAL_PROMPT="0"; $env:GCM_INTERACTIVE="never"`). Ветка `migration`
создана от `main` (29b25e8); все правки 21.09 разложены на атомарные коммиты. Прямой push в `main`
блокируется автоклассификатором → пушим `migration`, слияние в `main` — владелец через PR.

## Рабочий промпт (21.09)
Владелец утвердил операционный промпт «перенос 1-в-1, пункт за пунктом, с тестом на каждый пункт»:
Шаг 0 санитария git → Шаг 1 полная переинвентаризация старой платформы (маршруты, CRUD-поля и
фильтры, сервисы/хелперы, blade-шаблоны, config, таблицы, роли, mobile API, console/events/middleware,
validation) → Шаг 2 цикл по пунктам MIGRATION.md (перенос → тест старое-vs-новое → полный test-suite →
деплой на локальный стек + live → отметка → changelog → коммит → push) → Шаг 3 конфигурируемость (⚙–).

## Статус
- Шаг 1 брифа (MIGRATION.md) — заведён 21.09, ведётся; Шаг 0 промпта (git) — выполнен 21.09.
