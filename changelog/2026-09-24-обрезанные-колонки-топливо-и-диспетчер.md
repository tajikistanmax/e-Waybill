# 2026-09-24 — Пункт выдачи топлива и кабинет диспетчера: колонки таблиц больше не срезаются

## Что изменилось
- `/fuel`: список «Путевые листы на заправке» получил больше ширины (1,4 : 1 к панели выдачи) —
  столбец «Выдано, л» виден целиком.
- `/dispatcher`: таблица «Требуют внимания» прокручивается по горизонтали вместо обрезки, статус
  не переносится.

Файлы: `apps/web/app/fuel/page.tsx`, `apps/web/app/dispatcher/page.tsx`.

## Как было ДО
На экране 1440 px у заправщика столбец «Выдано, л» уезжал за край карточки (скрытая прокрутка) —
не видно, сколько топлива уже выдано по листу; у диспетчера бейджи статуса («Ожидает осмотров»,
«Ожидает оплаты») срезались краем карточки.

## Как стало
Оба столбца видны целиком (проверено скриншотами Playwright под `bus1.azs` и `bus1.disp`).

## Почему
Кабинеты заправщика и диспетчера показываются на демонстрации 24.09.

## Кто решил
Владелец 23.09.2026: «доведи платформу до запуска к демонстрации». Найдено обходом страниц
Playwright; исправлено агентом Claude (блок «путевые листы, расчёты, печать, отчёты»).

## Как проверено
`npx tsc --noEmit` — без ошибок; пересобран `web`; скриншоты `/fuel` и `/dispatcher`.

## Коммит
- `<hash>` — fix(web): колонки таблиц пункта топлива и кабинета диспетчера не срезаются

## Как откатить
`git revert <hash>` → пересобрать `web`.
