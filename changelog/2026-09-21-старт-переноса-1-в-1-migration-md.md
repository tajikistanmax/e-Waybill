# 2026-09-21 — Старт программы переноса 1-в-1: MIGRATION.md + начало верификации

## Что изменилось
Заведён управляющий документ **`MIGRATION.md`** (полный чек-лист переноса legacy «Роҳхат» → e-Waybill:
роли, справочники, формы ПЛ, жизненный цикл, расчёты, отчёты, шаблоны, фильтры, API, конфигурируемость)
со статусами покрытия/конфигурируемости и разделом «Вопросы к Максу». Бриф владельца сохранён в
`spec/notes/дальнейшая-работа.md`. Начата верификация: сверен пункт **2.3 organization** (поля
companies↔organization).

## Как было ДО
Отдельного управляющего чек-листа переноса не было (были россыпью `gap-plan.md`,
`legacy-full-inventory.md`, `our-platform-inventory.md`). Единой точки «что перенесено 1-в-1 /
что гэп / что конфигурируемо» не существовало.

## Как стало
`MIGRATION.md` в корне — единая точка ведения переноса (по образцу CLAUDE.md рядом). По пунктам
отмечается `[x]/[~]/[ ]` + ⚙ (конфигурируемость) + «как проверено». Синтезирован из готовых инвентаризаций
и прямой сверки. Итог первичной сверки: e-Waybill — зрелый порт (большинство `[x]`); работа =
верификация тестами + закрытие `[~]/[ ]` и захардкоженной конфигурируемости (гл. фокус — шаблоны/отчёты).

## Почему
Владелец поставил задачу перенести ВСЮ логику 1-в-1 с новым требованием — конфигурируемость шаблонов/
отчётов/настроек через UI. Нужен управляемый, проверяемый по пунктам план.

## Кто решил
Владелец (пользователь) поставил задачу и порядок работы (бриф); реализовал агент Claude.

## Как (техническое решение)
Документ-чек-лист + лог верификации в самом файле. Верификация 2.3: сравнение колонок БД обоих стендов
(legacy MySQL `companies` ↔ e-Waybill PG `organization`) + grep master-data. Найдены гэпы полей:
`number`(рамз), `give_fuel`, `seal_attach`, `status` → Вопрос 8 (безопасный дефолт — не добавлять, рамз=rma).

## Хэш коммита
- Документы (MIGRATION.md, бриф, эта запись) закоммичены 21.09 в ветке `migration` коммитом
  `docs(migration): …` — см. `git log --oneline -- MIGRATION.md`. Git в окружении есть
  (portable MinGit, см. запись `2026-09-21-санитария-репозитория-ветка-migration.md`).

## Как откатить
- Удалить `MIGRATION.md`, `changelog/2026-09-21-старт-переноса-1-в-1-migration-md.md`,
  `spec/notes/дальнейшая-работа.md` (только документы, кода не касается).
