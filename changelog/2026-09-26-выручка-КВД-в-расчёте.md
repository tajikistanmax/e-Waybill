# 2026-09-26 — Выручка и круги из B2B-канала КВД доходят до расчёта и отчётов

## Что изменилось
- `waybill-service/.../service/RefChannelService.java` — `returnMetrics`: закрытие листа 1-АД из канала КВД передаёт круги (`number_lap`), выручку (`earning`) и «гашти ибтидоӣ» (`begin_path_a/b`) теми же полями, что форма возврата диспетчера.
- `waybill-service/.../calc/WaybillCalcAssembler.java` — если у рабочих дней листа нет выручки, расчёт берёт её из данных канала: `typeData.earning` (1-АД), иначе `typeData.kassa` (1-А, 3-С).
- Тесты: `RefChannelReturnMetricsTest` (2), `WaybillCalcAssemblerConsignmentNotesTest.b2bEarning`.
- `spec/notes/08-…` — статус B1 (QR на `localhost`) исправлен на ✅: сделано 25.09, коммит «QR на печатных бланках и часовой пояс служб».

## Как было ДО
- **Круги.** При закрытии листа 1-АД через канал КВД `number_lap` записывался в «ездки Z» — грузовой показатель, который пассажирский расчёт не читает.
- **Выручка.** `earning` 1-АД и `kassa` 1-А/3-С сохранялись в данных листа, но расчёт их не видел.
- **Итог.** Отчёты по выручке и зарплата (доля выручки) у листов, пришедших из КВД, показывали 0 (сверка 25.09, A23).

## Как стало
- Лист 1-АД без рабочих дней получает при закрытии рабочий день с кругами и выручкой — так же, как при возврате диспетчером (решение Р2).
- У 1-А/3-С, если по дням выручки нет, берётся `kassa`, как legacy MBusCalc/BusCalc («даромад» = kassa).

## Почему
Сверка 25.09, A23. Интегратор КВД передаёт эти поля, а платформа их теряла.

## Кто решил
Задача — владелец (полная сверка). Источник выручки — по legacy.

## Как проверено
- waybill-service: 481 тест — 479 ✅, 2 Testcontainers (окружение). Новые тесты ✅.
- Регресс всех форм: 324/324. Регресс отчётов: 70/70.
- Живой прогон канала КВД не выполнялся: нужна отдельная учётка интегратора. Разбор тела запроса и чтение выручки проверены тестами.

## Коммиты
- `<hash>` — fix(КВД): круги и выручка 1-АД/1-А/3-С из B2B-канала — в показатели дня и расчёт

## Как откатить
`git revert <hash>` + пересборка `waybill`.
