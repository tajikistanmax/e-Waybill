# 2026-09-21 — Постраничный вывод реестров ТС / водителей / сотрудников

## Что изменилось
`apps/web/app/registry/RegistryView.tsx` рендерит только текущую страницу (20 строк),
добавлены кнопки ‹ ›, счётчик «страница / всего» и сброс на первую страницу при смене
фильтров или поиска.

## Как было ДО
Таблица реестра выводила ВСЕ отфильтрованные строки разом. После миграции Ф1–Ф4
(89 тыс. ТС, 68 тыс. водителей) браузер вешался при открытии `/registry/vehicles` и
`/registry/drivers`.

## Как стало
Страница открывается мгновенно; поиск и фильтры работают по полному набору, показывается
только срез. Загрузка данных с бэкенда не менялась (серверная пагинация реестров — отдельная задача).

## Почему
Боевой объём данных сделал реестры неработоспособными.

## Кто решил
Владелец — задача «продолжить», лёгкий вариант (клиентская пагинация) выбран как безопасный;
реализовал агент Claude.

## Затронутые файлы
- `apps/web/app/registry/RegistryView.tsx` — `PER_PAGE`, состояние `page`, срез `view`, навигация.

## Коммиты (ветка `migration`)
- `112ca7e` — fix(web): постраничный вывод реестров ТС/водителей/сотрудников (20 строк)

## Как откатить
- `git revert 112ca7e`.
