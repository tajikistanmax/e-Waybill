# 2026-09-24 — Повторная сверка старой и новой платформы, полный регресс

## Что изменилось
- `scripts/security-matrix.ps1`: +7 проверок — «Пользователи» платформы (только администратор
  платформы; админ компании, аналитик, диспетчер — 403) и изображения для бланка (подпись
  сотрудника инспектору доступна, паспорт инспектору и врачу — 403).
- Браузерные e2e (устарели после перехода на свой вход и обновления демо-данных — 22 из 75 падали):
  `apps/web/e2e/fixtures.ts` — настоящие демо-пароли (`PASSWORDS`, `loginAs`) вместо «пароль = логин»,
  для ролей со вторым фактором — учётки `*-automation`; `accountant/dispatcher/inspector/org-scope.spec.ts`
  — вход через `loginAs`; `org-scope.spec.ts` — под нынешнюю иерархию демо (филиалы «Юг» 100002091 и
  «Север» 100002092 у компании 025680800; чужой перевозчик 040000830 не виден); `dispatcher.spec.ts` —
  существующие ТС 9914QA01 и водитель 990000008, новая подсказка поиска реестра, созданный тестом лист
  отменяется (иначе правило «один открытый ПЛ» мешает повторному прогону).
- `scripts/e2e-roles-cabinets.ps1`: предупреждение Node в stderr больше не обрывает скрипт (итог — по коду
  выхода Playwright).
- `AUDIT.md` §6: п. 1 (перезалив архива) — свежие цифры сверки с legacy за июль 2026 и второй
  вопрос к решению (листы без РМА водителя); п. 3 (Keycloak) — закрыт полностью.

## Как было ДО
Матрица прав не проверяла новые точки 24.09. Пять старых браузерных спецификаций входили с паролем,
равным логину, и ждали прежние демо-данные — в полном прогоне падали 22 теста из 75. В AUDIT.md п. 3 числились невыполненными вывод
контейнера Keycloak и второй фактор (сделаны 24.09); в п. 1 не было оценки, насколько архив
меньше legacy.

## Как стало
Сверка по меню старой платформы (все 38 пунктов `sidebar_content.blade.php`, маршруты
`routes/backpack/*.php`, внешние API `routes/api.php`, планировщик `Console/Kernel.php`) с
реестром `MIGRATION.md`. Найдено и закрыто в этот день: страница «Пользователи» (legacy
`/admin/user`, коммит `c615789`), печать организации и подписи на бланке (`791a337`). Прочие
пункты меню — перенесены ранее (строки реестра 1–12). Открытым остаётся только то, что требует
решения владельца (AUDIT.md §6).

## Почему
Владелец 24.09.2026: «ещё раз полностью проверь старую и новую платформу, посмотри, что
пропустил… тестируй вживую… будь руководителем».

## Кто решил
Владелец — поручение; порядок и объём проверки — разработчик (Claude).

## Как проверено (стенд `epd-prod-*`, сборка текущей ветки)
- master-data — 199/199; waybill — 386/388 (2 интеграционных теста требуют Docker внутри
  контейнера сборки — так всегда).
- smoke 35/35; матрица прав 34/34; блокировки 5/5; конкурентность 4/4; отчёты 70/70; все формы
  ПЛ 324/324.
- Браузерные e2e (Playwright), весь набор: напрямую (:3000) — 75/75; через HTTPS-прокси — 75/75;
  `scripts/e2e-roles-cabinets.ps1` — 36/36, замечаний 0 (все пункты меню 14 демо-логинов, 88 страниц).
  До исправления тестов — 53/75: все 22 падения — неверный пароль в старых спецификациях (платформа
  отвечала «Неверный логин или пароль», как и должна); реальных ошибок платформы среди них нет —
  «админ компании видит филиал «Юг»» оказался корректным поведением на обновлённых демо-данных.
- Живая сверка чисел с базой legacy (дамп 07.08.2026): июль 2026, 1-А — 33 251 в legacy против
  13 081 у нас; 3-С — 42 081 / 19 846; 2-Б — 18 733 / 9 484; автобус — 10 145 / 9 245. Причина —
  находка 20 (перенос архива брал водителя ТС, а не листа, и отбрасывал листы без РМА водителя);
  исправленный скрипт вернул бы, например, по 1-А 19 696, а 13 511 листов 1-А без РМА водителя
  не переносятся ни при каком перезаливе. Решение — за владельцем (массовое удаление архива).

## Коммиты
- `3b6c42e` — test: матрица прав и e2e под текущий вход и демо-данные; сверка архива в AUDIT.md

## Как откатить
`git revert 3b6c42e` (только скрипт проверки и документы; код платформы не меняется).
