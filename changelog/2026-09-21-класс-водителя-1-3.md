# 2026-09-21 — Водитель: класс (дараҷа) ограничен диапазоном 1–3 (MIGRATION.md §12.7)

## Что изменилось
- master-data `POST /api/v1/drivers`: `degree` — `@Min(1) @Max(3)` («Класс водителя: 1–3»); пусто по-прежнему допустимо.
- Тест `DriverRequestValidationTest` (2): 1, 3, пусто — ок; 0 и 4 — нарушение с текстом.

## Как было ДО
Legacy (§12.7): `degree ∈ 1..3` (класс водителя, питает надбавку `cat_1/2/3` в зарплате — §5.5). В e-Waybill поле
принимало любое число: класс 7 ломал бы расчёт зарплаты (нет `cat_7` → надбавка 0 без ошибки).

## Как стало
Диапазон как в legacy. Остальные диапазоны §12.7 сверены ранее: transport_type 1–6, employee.type 1–5 (2.27),
region 1–7 (организация), fuel 1–5; регионы 3-С 1–7 — через организацию.

## Почему
MIGRATION.md 12.7 — последний непроверенный диапазон legacy.

## Кто решил
Агент Claude по промпту Шага 2 (однозначное правило).

## Как проверено
- `DriverRequestValidationTest` (2); `:master-data-service:test` **74/74 ✅**.
- **Live (локальный стек, org 025680800):** `POST /drivers` с `degree=4` → HTTP 400; с `degree=2` → HTTP 201, в ответе `degree=2`.

## Затронутые файлы
- `apps/backend/master-data-service/src/main/java/tj/mintrans/epd/masterdata/web/DriverController.java`.
- `apps/backend/master-data-service/src/test/java/tj/mintrans/epd/masterdata/web/DriverRequestValidationTest.java` — новый.
- `MIGRATION.md` — 12.7 → [x].

## Коммиты (ветка `migration`)
- `a6028e6` — feat(master-data): класс водителя (degree) ограничен диапазоном 1–3 (§12.7)

## Как откатить
- `git revert <hash>` + пересборка `master-data`.
