# 2026-09-25 — Визуальная проверка всех страниц: исправлены обрезанные таблицы, шапка, плитки

## Что изменилось
- `apps/web/app/waybills/page.tsx` — реестр путевых листов: таблица прокручивается внутри
  карточки (плотная); у листа без номера — «без номера», «черновик» — только у настоящего черновика.
- `apps/web/app/consignments/page.tsx`, `apps/web/app/violations/page.tsx` — то же для «Накладных»
  и «Нарушений».
- `apps/web/app/topbar.tsx`, `apps/web/app/globals.css` — шапка: названия страниц «Пользователи»,
  «История осмотров», «Журнал техконтроля», «Накладные», «Уведомления» (было общее «Электронный
  путевой лист»); заголовок и дата в одну строку, переключатель филиалов фиксированной ширины,
  дата скрывается на экранах уже 1400 px.
- `apps/web/app/dictionaries/layout.tsx` — плитки справочников переносятся по ширине экрана.
- `apps/web/app/waybills/new/page.tsx` — мастер: карточки видов ПЛ с пометкой «Недоступно»
  не налезают на правую панель.
- `apps/web/app/settings/backup/page.tsx`, `packages/shared/lib/i18n.tsx` — страница «Резервное
  копирование» описывает то, что реально работает (везде «Действует»), вместо «Планируется».

## Как было ДО
Найдено при просмотре 181 скриншота всех страниц 14 ролей (обход `e2e-roles-cabinets`, экран 1280):
- **реестр путевых листов**: карточка обрезала таблицу (`overflow: hidden`) — на экране 1280
  колонки «Статус» и кнопка «Открыть» были не видны и не прокручивались; то же в «Накладных»
  (таможня, контрагенты), в «Нарушениях» обрезался «Статус»;
- у администратора компании (с переключателем филиалов) название страницы и дата в шапке
  переносились в 2–3 строки;
- пятая колонка плиток «Справочников» уходила за правый край;
- в мастере нового ПЛ у организации без разрешённых видов карточки с пометкой «Недоступно для
  вашей организации» налезали на панель «Параметры создания»;
- на пяти страницах в шапке было «Электронный путевой лист» вместо названия раздела;
- «Резервное копирование» писало «Планируется» про копирование, шифрование и восстановление,
  хотя служба копий работает (ночная копия 25.09 00:00 — обе базы, AES-256) и учение по
  восстановлению проведено 24.09;
- отменённый лист без номера в реестре подписывался «— черновик —».

## Как стало
Все таблицы помещаются или прокручиваются внутри карточки, шапка в одну строку, плитки
переносятся, тексты страниц соответствуют действительности. Проверено повторным обходом со
скриншотами.

## Почему
Владелец 25.09.2026: «живой тест тебе нужно было сделать, чтобы визуально смотрел, как выглядит
платформа и где что как работает».

## Кто решил
Владелец — поручение; что считать дефектом и как исправить — разработчик (Claude).

## Как
Таблицам — обёртка `overflowX: auto` и класс `dense`; сетке мастера — `repeat(2, minmax(0, 1fr))`;
плиткам — `repeat(auto-fill, minmax(180px, 1fr))`; шапке — `white-space: nowrap` + многоточие,
`@media (max-width: 1400px)` скрывает дату.

## Как проверено
`tsc --noEmit` — без ошибок; web пересобран; `scripts/e2e-roles-cabinets.ps1 -Only cabinets` —
23 из 24. Один тест не вошёл под `accountant`: его пароль 25.09 в 11:40 сбросили вручную из
другого браузера (Chrome 153, не тестовый) через «Пользователи» — это видно по журналу аудита,
ошибки платформы нет. Скриншоты исправленных страниц просмотрены: шапка в одну строку, названия
разделов, плитки, «без номера», прокрутка таблиц.

## Коммиты
- `f8d8eb6` — fix(web): визуальные дефекты по просмотру всех страниц

## Как откатить
`git revert f8d8eb6` + пересборка `web`.
