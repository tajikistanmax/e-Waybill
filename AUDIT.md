# AUDIT.md — сквозная приёмка «от А до Я» перед продакшеном

> **Заказ владельца (22.09.2026):** проверить каждый пункт, каждую страницу, каждую роль, каждую графу,
> каждый лог; сверить, как работало в старой платформе и как работает в новой; всё, что пропущено —
> доделать; где логика неверна — исправить (в том числе там, где в legacy неправильно или сделано
> вручную); довести платформу до идеального состояния для продакшена. Работать самостоятельно, не
> дожидаясь одобрения, вести задачи и тесты самому, не останавливаться до полного завершения.

**Старое:** `D:\Projects\rohkhat.tj ralavel` (PHP/Laravel + Backpack), стенд http://localhost:8000 — **только чтение**.
**Новое:** e-Waybill, локальный стек `epd-prod-*` — web http://localhost:3000, master-data :8081, waybill :8082, Keycloak :8180.
**Ветка:** `migration`. **Сервер 10.10.29.70 не трогать** (деплой — только по явной команде).

---

## 0. Рабочий промпт (как я работаю в этом заходе)

Цикл на каждый пункт реестра (§2). Пункт не закрывается, пока не пройдены все шаги.

1. **Прочитать эталон.** Открыть соответствующий код legacy целиком (контроллер + модель + blade + trait),
   выписать: поля, фильтры, валидации, вычисления, права, побочные эффекты.
2. **Прочитать наш код целиком** (контроллер + сервис + сущность + миграция + страница web).
3. **Сверить построчно** и записать расхождения тремя типами:
   - **ГЭП** — в legacy есть, у нас нет → перенести;
   - **БАГ** — у нас есть, но работает неверно/ломается → исправить;
   - **ЛЕГАСИ-БАГ** — в legacy неправильно или сделано руками → **не копировать**, сделать правильно
     и записать в §3 с обоснованием.
4. **Исправить** (backend + Flyway + API + web + печать — всё, чего требует пункт).
5. **Написать тест** на исправленное поведение (JUnit и/или проверка типов web).
6. **Прогнать полные наборы:** `:master-data-service:test`, `:waybill-service:test`, `npx tsc --noEmit`.
7. **Выложить на локальный стек** (`docker compose … up -d --no-deps <svc>`), дождаться healthcheck.
8. **Проверить вживую** под нужной ролью: запрос/ответ или страница; записать фактические числа и коды.
9. **Прочитать логи** контейнеров после проверки (`docker logs --since`) — ни одного ERROR/WARN,
   связанного с проверяемым пунктом; найденные ошибки лечить, а не замалчивать.
10. **Отметить строку** реестра `[x]` с колонкой «как проверено» (что именно выполнено и что получено).
11. **Журнал изменений** `changelog/YYYY-MM-DD-<имя>.md` (что / как было ДО / как стало / почему /
    кто решил / как / хэш / как откатить) + строка в `changelog/README.md`.
12. **Атомарный коммит** на русском + `git push origin migration`.

**Запреты:** менять legacy; трогать сервер 10.10.29.70 и его БД; `push --force`, `reset --hard`;
массовые UPDATE/DELETE (>1000 строк) без разрешения; пересоздавать `MEDDATA_ENCRYPTION_KEY`;
писать «проверено», не выполнив проверку.

**Непонятное:** записать «Вопрос к Максу» в `MIGRATION.md`, выбрать самый безопасный вариант и идти дальше.

### Что считается «проверено»
| Уровень | Обязательно для |
|---|---|
| код сверен построчно | каждый пункт |
| автотест | любое изменение логики |
| полные наборы тестов зелёные | каждый коммит |
| живой запрос с фактическим ответом | каждый пункт с API |
| открытая страница под нужной ролью | каждый пункт с UI |
| логи без ошибок | каждый пункт |

---

## 1. Учётные записи стенда (для проверок под ролями)

| Роль | Логин | Организация |
|---|---|---|
| SYSTEM_ADMIN | `admin` | — (все) |
| COMPANY_ADMIN | `company` | 025680800 |
| BRANCH_ADMIN | `branch` | 025680800 / филиал |
| DISPATCHER | `dispatcher` | 025680800 |
| DOCTOR | `doctor` | 025680800 |
| MECHANIC | `mechanic` | 025680800 |
| DRIVER | `driver` | 025680800 |
| ACCOUNTANT | `accountant` | 025680800 |
| INSPECTOR | `inspector` | — |
| MINTRANS_ANALYST | `analyst` | — |
| FUEL_STATION | `fuel` | 025680800 |
| CLIENT_SENDER | `sender` | клиенты из `client_ids` |
| CLIENT_FORWARDER | `forwarder` | клиенты из `client_ids` |
| CUSTOMS_OFFICER | `customs` | все накладные |

Пароли — в `infra/keycloak/epd-realm.json` (стендовые, вида `Epd-Qa-*-2026`). Клиент `epd-web` — public,
grant `password`.

---

## 2. Реестр проверок

Обозначения: **[ ]** не проверено · **[~]** проверено, есть находки в работе · **[x]** закрыто.

### A. Роли и доступы

| # | Что проверяется | Ст. | Как проверено |
|---|---|---|---|
| A1 | Вход каждым из 14 логинов, выдача токена, состав claim (`organizationRma`, `rma`, `client_ids`) | [x] | 11 логинов входят по паролю; `admin`, `inspector`, `analyst` требуют TOTP (так задумано) — для автопроверок используются дубли `-automation` |
| A2 | Стартовая страница каждой роли (`roleHome`) открывается без редиректа в никуда | [x] | Стартовый раздел каждой из 14 ролей в матрице совпадает с кодом: бухгалтер — отчёты, врач — медосмотр, механик — техконтроль, водитель — свой кабинет, диспетчер — выдача, инспектор — контроль, пункт топлива — топливо, внешние кабинеты — накладные, остальные — панель |
| A3 | Меню каждой роли = `visibleNav` + матрица `role_access`; лишних пунктов нет | [x] | Матрица сверена с кодом построчно. Расхождение одно: роли «Пункт выдачи топлива» в таблице не было (13 строк из 14) — добавлена миграцией `V74`. Раздел «Компания» у перевозчика в матрице есть намеренно (прикрепление парка — его задача) |
| A4 | Каждая страница под каждой ролью: 200 либо осмысленный запрет (не пустой экран, не 500) | [x] | Все 78 адресов интерфейса отдают 200; разграничение разделов — по матрице, карточка путевого листа доступна по прямой ссылке и тем ролям, у кого нет пункта меню |
| A5 | Каждый API-эндпоинт под каждой ролью: разрешено ровно то, что задумано (403/404 иначе) | [x] | Прогон 80 эндпоинтов × 14 ролей: ни одного 500; все 400 — недостающие параметры с внятным сообщением («Required parameter 'X' is not present»). Находки 5 и 10 закрыты |
| A6 | Тенант-изоляция: чужая организация → 404, не 403; аналитик/инспектор — только чтение | [x] | Реестр чужой организации → 0 строк; выдача топлива на чужой ПЛ → 404 «Путевой лист не найден»; приём ПЛ чужим водителем → 403 |
| A7 | Область филиала (BRANCH_ADMIN) сужает списки парка, ПЛ, отчётов | [x] | Администратор филиала видит одну организацию — свой филиал (100002091), 0 ТС и 0 листов; администратор компании — свою (025680800), 31 ТС и 619 листов. Случай «компания + её филиалы» на стенде не воспроизводится: филиал принадлежит другому предприятию |
| A8 | Внешние кабинеты (`sender`/`forwarder`/`customs`) видят только свои накладные | [x] | Грузоотправитель видит 2 накладные своего контрагента, экспедитор — 1, таможня — все 3 международные; область в ответе подписана (`sender`/`forwarder`/`customs`) |
| A9 | Блокировка пользователя (`enabled=false`) и требование смены пароля | [x] | Полный круг на временной учётной записи: создание с временным паролем, вход требует смены пароля, блокировка, разблокировка, сброс пароля, смена роли, удаление; после удаления вход → 401. Учётная запись удалена |

### B. Страницы веб-интерфейса (каждая — поля, кнопки, пагинация, фильтры, пустое состояние, ошибки)

| # | Страница | Эталон legacy | Ст. | Как проверено |
|---|---|---|---|---|
| B1 | `/login`, `/auth/callback` | форма входа Backpack | [x] | Вход 11 ролями; публичные данные страницы входа (контакты, логотип) отдаются без токена |
| B2 | `/dashboard` | `dashboard`, `dashboard/ebus` | [x] | Счётчики статусов отдают разложение по 15 статусам; сводка предприятия — 619 листов |
| B3 | `/dispatcher` | список ПЛ диспетчера | [x] | Журнал за дату, выписка листа, замена водителя и ТС — через сквозной сценарий |
| B4 | `/waybills` (реестр, фильтры, пагинация) | `waybill` | [x] | Постраничный реестр (`/waybills/page`), счётчики по статусам, фильтры по периоду |
| B5 | `/waybills/new` (все 6 форм) | `waybill1ad/1a/2b/3c/5bbm/…` | [x] | Доступные виды с причинами недоступности; выписка пассажирской и грузовой формы; отказ 422 при отсутствии обязательного вида перевозки |
| B6 | `/waybills/[id]` (карточка, история, действия) | карточка ПЛ | [x] | История переходов (8 записей за цикл), титулы Т1–Т6, расчёт, действия по статусам |
| B7 | `/waybills/[id]/print` + печатные бланки | blade-шаблоны | [x] | Пассажирский бланк 46 031 байт, грузовой 35 830, приложение к накладной 36 507, СМР 42 059; QR 493 байта. CMR по форме 2-Б → 422 (бланк только для 5Б-БМ) |
| B8 | `/waybills/journal` | — | [x] | Журнал диспетчера за дату отдаёт строки |
| B9 | `/med`, `/med/journal` | осмотр врача | [x] | Допуск Т2 и послерейсовый Т6; журнал врача за период + выгрузка XLSX (28 КБ) |
| B10 | `/tech`, `/tech/journal` | осмотр механика | [x] | Допуск Т3 с одометром; журнал механика + выгрузка XLSX (39 КБ) |
| B11 | `/fuel` | выдача топлива | [x] | Предзаполнение остатка и запись выдачи; лист чужой организации → 404 |
| B12 | `/driver`, `/driver/waybills` | мобильное API | [x] | Карточка водителя, его листы, приём листа (→ «выдан»); чужой лист → 403 |
| B13 | `/inspector` | проверка ПЛ | [x] | Доступ к карточке листа и нарушениям; экономика перевозчика закрыта (403 на топливо и разрезы) |
| B14 | `/consignments` | накладные клиента | [x] | Отправитель видит 2 накладные, экспедитор 1, таможня 3; правка, печать СМР (42 КБ), таможенное подтверждение; состав ответа сужен до полей накладной |
| B15 | `/fleet/vehicles`, `/fleet/drivers`, `/fleet/employees` | `parking`, `driver`, `employee` | [x] | Парк перевозчика: 31 ТС, 30 водителей, 5 сотрудников своей организации; поиск по части госномера и Ф.И.О. |
| B16 | `/company` (реестр организаций, прикрепления, документы) | `company` | [x] | Реестр организаций (604), прикрепление и открепление парка, документы организации и субъектов |
| B17 | `/company/access` (пользователи, роли, кабинеты) | `user`, `role` | [x] | 8 пользователей организации с ролями; полный круг провижининга (см. A9) |
| B18 | `/registry/*` (vehicles, drivers, employees, devices) | реестры | [x] | Постраничные реестры: 89 287 ТС, 68 006 водителей, 72 сотрудника; поиск, фильтры по виду, должности, региону, городу и предприятию; выгрузка CSV |
| B19 | `/violations` | нарушения | [x] | Раздел открыт инспектору, аналитику и перевозчику; страница отдаёт 200 |
| B20 | `/monitoring` | `gpsevent` | [x] | Живые точки по действующим листам; журнал событий пополняется, пауза 20 минут соблюдается |
| B21 | `/reports/*` (9 страниц) | `report*` | [x] | См. блок F: 24 сочетания разрезов, сводные отчёты, журналы, 8 выгрузок XLSX |
| B22 | `/dictionaries/*` (17 справочников) | CRUD-справочники | [x] | См. блок C: полный круг «создать → править → удалить» по каждому |
| B23 | `/settings/*` (21 страница) | конфиг + хардкод legacy | [x] | Все страницы отдают 200; матрица ролей, виды ПЛ с отключением, политики, сроки документов, аудит, шаблоны печати проверены запросами |
| B24 | `/notifications` | уведомления | [x] | Лента уведомлений и счётчик непрочитанных (32) |
| B25 | `/verify/[jws]` | QR-проверка | [x] | Проверка **без токена** возвращает подпись, реквизиты листа и его текущий статус; подделанный QR отклоняется с сообщением «Подпись недействительна» (422); страница проверки отдаёт 200; журнал сканов пополняется (9 записей) |
| B26 | Общее: сворачивание меню, локализация (ru/tj), пустые состояния, подтверждения | — | [~] | Проверяется глазами: требует визуального просмотра владельцем |

**Сквозная проверка страниц:** все **78** адресов интерфейса отдают 200, в журналах веб-приложения,
мастер-данных и службы путевых листов за время прогона нет ни одной ошибки.

### C. Справочники (поля 1-в-1, CRUD, просмотр/правка/удаление, валидация, пагинация, поиск)

| # | Справочник | Legacy | Ст. | Как проверено |
|---|---|---|---|---|
Полный круг «создать → прочитать в списке → изменить → удалить» прогнан по каждому справочнику
под системным администратором; отдельно проверено, что перевозчик получает отказ там, где
справочник национальный. Тестовые записи удалены, остатков в базе нет.

| C1 | Маршруты | `route` | [x] | создание, правка названия, удаление; перевозчик правит свои маршруты (200) — так задумано |
| C2 | Виды маршрутов | `routetype` | [x] | создание и удаление |
| C3 | Направления | `directions` | [x] | создание, правка номера, удаление; перевозчику 403 |
| C4 | Клиенты | `client` | [x] | создание, смена вида клиента 1→2, удаление |
| C5 | Грузы | `cargo` | [x] | создание, правка цены 10,5→12,5, удаление |
| C6 | Тарифы / тарифы маршрута | `tariff` | [x] | правка тарифа за км 4,25→4,75, удаление; перевозчику 403 |
| C7 | Нормы топлива, марки | `brand`, `fuel` | [x] | норма 30,5→31,5; марка с правкой расхода 30→35; перевозчику 403 |
| C8 | Коэффициенты зима/город/горы/износ | `wintercoef`,`citycoef`,`mountaincoef`,`usedcoef` | [x] | все четыре: создание, правка значения, удаление; перевозчику 403 |
| C9 | Классы вождения | `driverclass` | [x] | создание, правка надбавки 15→20, удаление |
| C10 | Города / регионы | `city`, `region` | [x] | город: создание, переименование, удаление, перевозчику 403; регионов 7 |
| C11 | Внешние города и страны | `external_cities`, `external_countries` | [x] | город создан и удалён; классификатор стран — 218 записей |
| C12 | Маълумотнома и её маршруты | `malumotnoma`, `routemalumotnoma` | [x] | чтение и отчёт по справкам, выгрузка XLSX |
| C13 | Планы ПЛ | `waybill_plan` | [x] | читает Минтранс и системный администратор, перевозчику 403 |
| C14 | Виды ПЛ | `billtypes` | [x] | `available-types` возвращает 10 видов с причинами недоступности отключённых |
| C15 | Устройства | `phone` | [x] | создание и удаление устройства водителя |

### D. Бизнес-процесс путевого листа (по каждой из 6 форм отдельно)

| # | Этап | Ст. | Как проверено |
|---|---|---|---|
| D1 | Выписка: обязательные поля, подстановки, запреты (просрочки, отстранение, занятость) | [x] | Второй лист на то же ТС → 409 «уже оформлен действующий путевой лист»; форма 2-Б без вида перевозки → 422 с указанием допустимых значений; отключённые типы ПЛ помечены в `available-types` |
| D2 | Медосмотр: допуск/недопуск, причина, подпись, влияние на статус | [x] | Предрейсовый Т2 → `medPassed=true`; послерейсовый Т6 в статусе «возвращён»; осмотр в статусе «готов» → 409 «Медосмотр невозможен в статусе READY» |
| D3 | Техконтроль: допуск/недопуск, одометр, подпись | [x] | Т3 переводит лист в «ожидает оплаты», `techPassed=true` |
| D4 | Выдача топлива: остаток, выдано, норма, факт | [x] | Предзаполнение остатка (`found=false` при первом листе ТС); запись выдачи 100 л при остатке 20 л |
| D5 | Работа на линии: рейсы, дни, пассажиры, груз | [x] | Рабочий день (4 рейса, выручка 1500,50) сохранён; накладная по грузовому листу принята |
| D6 | Закрытие: одометр, расход, простои, переработка | [x] | Закрытие без Т6 → 409 «Требуется послерейсовый медосмотр»; после Т6 → «закрыт»; одометр ТС в справочнике обновился 0 → 260 |
| D7 | Оплата/подтверждение, блокировка, аннулирование | [x] | Бухгалтер подтверждает оплату → «готов» и присвоен номер `01-26-04-0000141-7`; аннулирование с причиной работает |
| D8 | Статусная модель и запрещённые переходы | [x] | Каждая операция вне своего статуса даёт 409 с указанием текущего и требуемого статуса (выезд, возврат, закрытие, рабочий день, расчёт) |
| D9 | Нумерация листов, уникальность, сквозные номера | [x] | Номер присваивается при переходе в «готов»; формат `филиал-год-тип-порядковый-контроль` |
| D10 | Накладные (прил. 1 и 2), CMR | [x] | Накладная к 2-Б принята, приложение печатается (36 507 байт); CMR по 2-Б → 422 (бланк только для 5Б-БМ); кабинет таможни видит 3 международных листа |

### E. Расчёты (числа должны совпадать с legacy на одних входных данных)

| # | Расчёт | Legacy-источник | Ст. | Как проверено |
|---|---|---|---|---|
| E1 | Норма топлива по марке + коэффициенты (зима/город/горы/износ/кондиционер) | `helpers.php` | [x] | Формула сверена с эталоном построчно: `K = (зима + горы + остановки + город + износ) − качество дороги`, множитель `1 + 0,01K`. Живой расчёт: 160 км, база 32 л/100 км, городской коэффициент 5 % → 51,2 × 1,05 = **53,76 л** — ровно столько вернул сервис |
| E2 | Остаток топлива между листами | `parking_fuel_left` | [x] | Предзаполнение остатка отвечает «предыдущего листа нет» на первом листе ТС и подставляет остаток, когда он есть |
| E3 | Пробег, в т.ч. с грузом; тонно-километры | расчёт ПЛ | [x] | Пробег = разность одометров (160 и 700 км в двух сценариях); транспортная работа принимается при возврате |
| E4 | Выручка по тарифу/маршруту, `adv_coe` | `TariffMath` | [x] | Стоимость рейса: 160 × 2,5 = **400,00**; 700 × 3,0 = **2100,00**. `adv_coe` в расчёт не входит — как в эталоне |
| E5 | Пассажиропоток, ADT | `AdtController` | [~] | Формулы перенесены, но на перенесённых листах показатели нулевые — нет исходных данных (находка 11) |
| E6 | Зарплата/начисления водителя | legacy-поля | [~] | Разрез «заработок водителей» строится, суммы нулевые по той же причине |
| E7 | Округления и единицы измерения | — | [x] | Литры и деньги — с двумя знаками (53,76; 2100,00); километры — целые |
| E8 | **Ошибка расчёта в эталоне не перенесена** | `helpers.php:379` | [x] | В старой платформе надбавка на кондиционер из-за приоритета операторов равна просто доле времени работы кондиционера, а не расходу топлива. У нас применена правильная формула, а поведение эталона сохранено отдельным методом для сверки |

### F. Отчёты (сверка с legacy на одном периоде и одной организации)

| # | Отчёт | Legacy | Ст. | Как проверено |
|---|---|---|---|---|
| F1 | Сводный по формам ПЛ (12 разрезов) | `/report` | [x] | Все 12 разрезов × 2 вида (пассажирский/грузовой) = 24 сочетания отдают данные за 0,5–5 с. Контроль: за 01–03.07 пассажирских листов 4070, строк «по ТС» 3441, «по водителям» 2813 — точно совпадает с числом листов и различных ТС/водителей в базе |
| F2 | Детализация | `/report_details` | [~] | Покрыта разрезом «реестр путевых листов» (построчно по документам) |
| F3 | Грузовые ПЛ | `/reportwaybillcargo` | [x] | Те же 12 разрезов с видом «грузовой»: 1099 листов, 1065 ТС, 908 водителей за 01–03.07 |
| F4 | Общий по грузовым | `/reportwaybillgeneral` | [x] | Сводный отчёт перевозок, вид «CARGO» |
| F5 | Справки (маълумотнома) | `/reportmalumotnoma` | [x] | Отчёт по кассирам отдаёт данные; отличие от legacy — нет колонки «кто изменил» |
| F6 | Региональный/ведомственный | `waybillcargogeneral/form` | [x] | Счётный: 7 регионов, 2–17 городов, сумма 52 895 = число листов июля в базе. Перевозок: строится за 47 с (был 503) |
| F7 | Топливо, по водителям, по ТС, журналы | наши | [x] | Разрезы по топливу (сводно, по листам, по ТС, по водителям) отдают строки; журналы врача и механика — 200 |
| F8 | Выгрузка XLSX/PDF каждого отчёта | — | [x] | 8 выгрузок XLSX отдают корректный файл (сигнатура `PK`): пассажирский 150 КБ, грузовой 57 КБ, журнал врача 28 КБ, журнал механика 39 КБ, сводный перевозок 12 КБ, счётный сводный 30 КБ, норматив выдачи 5,6 КБ, справки 3,8 КБ. Печать ПЛ — 46 КБ, приложение к накладной — 36,5 КБ |
| F9 | 13 и 14 разрезы legacy (журналы механика и врача) | `/report` тип 13/14 | [x] | У нас отдельные эндпоинты журналов, а не разрез — паритет по содержанию |

### G. API и интеграции

| # | Блок | Ст. | Как проверено |
|---|---|---|---|
| G1 | B2B `ref/*` (6 форм, confirm, remain_fuel) | [x] | Все шесть форм отдают выборку в формате старой платформы (`current_page`, `data`, `per_page`, `total`); `remain_fuel` по госномеру возвращает остаток |
| G2 | Мобильное API водителя (`get_waybill`, QR) | [x] | `mobile/me` отдаёт карточку водителя и организацию; `mobile/waybills` — его листы; приём листа переводит в «выдан»; чужой лист → 403 «выписан на другого водителя»; QR — 493 байта |
| G3 | GPS: события, cooldown, направления | [x] | Событие «выезд на маршрут» принято и связано с действующим листом; повтор через секунду → 422 «возможна только через 20 минут»; недопустимое состояние → 422 со списком допустимых; журнал диспетчера пополнился |
| G4 | Агрегатор, вебхук оплаты | [~] | Подтверждение оплаты бухгалтером проверено в сквозном сценарии; внешний вебхук платёжного шлюза не проверялся |
| G5 | Провижининг пользователей (Keycloak admin) | [~] | Список и роли читаются администратором компании; создание пользователя не проверялось (создаёт учётную запись в Keycloak) |
| G6 | Проверка по QR/JWS, журнал сканов | [x] | Публичная проверка без токена работает и отдаёт живой статус документа; подделка отклоняется; журнал сканов ведётся |
| G7 | Коды ошибок и сообщения (совпадение с legacy там, где важно) | [x] | Сообщения о нарушении порядка статусов, паузе GPS и видах перевозки — на русском, с указанием текущего состояния и допустимых значений |

### H. Данные, миграции, логи, инфраструктура

| # | Блок | Ст. | Как проверено |
|---|---|---|---|
| H1 | Flyway master-data (V74) и waybill (V28) применяются с нуля | [ ] | |
| H2 | Ограничения БД: уникальность, внешние ключи, NOT NULL | [x] | Уникальны: ИНН организации, водителя и сотрудника, госномер ТС, пара «регион + код города», номер путевого листа (дублей в базе 0), пара «лист + дата» рабочего дня. Частичные индексы `uq_active_waybill_vehicle` и `uq_active_waybill_driver` не дают выписать второй действующий лист — это и даёт 409 при выписке. Для отчётов есть индексы по дате, по организации с датой и по статусу с датой |
| H3 | Логи всех контейнеров без ERROR при полном прогоне сценариев | [x] | После прогона 78 страниц, сквозных сценариев и справочников в журналах веб-приложения, мастер-данных и службы листов — ни одной ошибки |
| H4 | Healthcheck, перезапуск, готовность к деплою | [x] | Пять пересборок и перезапусков служб за время приёмки; после каждого стек поднимался и отвечал |
| H5 | Аудит действий пишется и читается | [x] | Журнал доступен только системному администратору (остальным 403); записи о создании, правке и удалении справочников появляются при каждом круге проверки |

---

## 3. Находки и исправления

| № | Блок | Тип | Находка | Решение | Коммит |
|---|---|---|---|---|---|
| 1 | B18 | БАГ | Реестры отдавали таблицу целиком: ТС 87 МБ / 42 с, водители 56 МБ / 27 с (+ весь справочник ТС ради колонки «Номер транспорта»). Страница администратора не открывалась на боевом объёме | Серверная пагинация `/{vehicles,drivers,employees}/page`, отбор и поиск на сервере. Стало 19,7 КБ / 242 мс | `3dc6a05` |
| 2 | H2 | БАГ | Географический отбор падал с `function lower(bytea) does not exist` (null-строка без типа в JDBC) | Явные ветки запроса вместо «или null» для строковых параметров | `3dc6a05` |
| 3 | A1 | не баг | `admin`, `inspector`, `analyst` не входят по паролю: `Account is not fully set up` | Так и задумано — у этих ролей включён обязательный TOTP. Для автопроверок есть дубли `admin-automation`, `inspector-automation`, `analyst-automation` | — |
| 4 | A5 | **БАГ** | Ограничитель запросов считал по IP: 300/мин на весь узел. За общим внешним адресом предприятия лимит делили все сотрудники; одна страница делает ~10 запросов | Счёт по пользователю (300/мин) + внешняя граница по адресу (3000/мин). Проверено: первый исчерпал лимит на 301-м запросе, второй с того же адреса получил 200 | `40c567b` |
| 5 | A5 | недочёт | Внешние кабинеты (`sender`/`forwarder`/`customs`) получали 200 на реестрах ТС/водителей/сотрудников. Утечки нет — список пуст, но и доступа быть не должно | Закрыто списком ролей `Authorities.REGISTRY_READ`: внешним кабинетам 403, рабочим местам 200 | `3180ea3` |
| 16 | A3 | ГЭП | В матрице доступа разделов было 13 ролей из 14: строки «Пункт выдачи топлива» не было, и настроить ей меню на странице «Роли» было нельзя (интерфейс работал по запасному пути в коде) | Миграция `V74` добавила строку; в матрице 14 ролей | `40c567b` |
| 6 | A5 | риск | Неограниченные списки `GET /api/v1/{vehicles,drivers,employees}` остаются (87/56 МБ): нужны интеграциям, но это нагрузка на узел | Оставлено сознательно (интегратор не должен молча получить обрезанную выгрузку); кандидат на выделение отдельного потокового канала выгрузки | — |
| 7 | F | **БАГ** | Отчёты считали «отработанным» только статус «закрыт». 2,28 млн перенесённых листов лежат в архиве, поэтому любой прошедший период показывал нули: сводка за июль 2026 — «52 895 листов, пробег 0» | `WaybillStatus.FINISHED = {COMPLETED, ARCHIVED}` во всех отчётах. Стало 11 606 606 км, сходится с контрольным SQL | `3180ea3` |
| 8 | F6 | **БАГ** | Сводный отчёт Минтранса падал с 503: расчёт запрашивал марку ТС и тарифы маршрута на каждый лист и упирался в ограничитель частоты | Справочники берутся одним запросом и ищутся в памяти (кэш 60 с). Отчёт строится за 47 с | `3180ea3` |
| 9 | F6 | **ГЭП** | Разрез «регион → город → предприятие» сводился к «Без региона»: снимок организации в перенесённом листе несёт только ИНН и название; город был заполнен у 8 организаций из 604 | Запасной источник географии из справочника + миграция `V73` (город 392 организациям). Стало 7 регионов, 2–17 городов | `3180ea3` |
| 10 | A5 | недочёт | Реестры парка и персонала отвечали 200 кабинетам грузоотправителя, экспедитора и таможни | `Authorities.REGISTRY_READ` — список ролей; внешние кабинеты получают 403 | `3180ea3` |
| 11 | H | **ГЭП данных** | Миграция не привезла показатели работы: рейсы (`number_lap`), выручку (`earning`), кассу (`kassa`), топливо (`fuel_given`). Для июля 2026 в источнике 10 347 листов с рейсами и 4 205 с выручкой на 2 512 559,01 — у нас нули | Дозагрузка = массовое изменение 2,28 млн строк. **Нужно решение владельца** | ожидает |
| 12 | C/H | легаси-баг | ИНН (РМА) организации: в источнике 676 компаний, валидных 9–10 цифр только 460, пустых 181. У нас 191 организация из 604 с невалидным ИНН (`13345`, `1`, `123`, `0101`) — их карточку нельзя сохранить, форма требует 9–10 цифр | Валидацию не ослаблять. Нужен список сопоставления «организация → настоящий ИНН» от владельца | ожидает |
| 13 | F6 | риск | Сводный отчёт перевозок за месяц строится 47 с (расчёт по каждому из 53 тыс. листов) | Приемлемо для месячного отчёта; ускорять после дозагрузки показателей (находка 11) | — |
| 14 | A8 | **ПДн** | Кабинет накладных отдавал путевой лист целиком: снимки организации (банк, лицензия, доля дохода), ТС и водителя (**паспорт**, права, срок медсправки). Внешней стороне нужна накладная, а не вся карточка | Отдельное представление из 13 полей: номер, форма, статус, срок, название предприятия, госномер, Ф.И.О. водителя, поля накладной. Снимков нет | `eb97508` |
| 15 | D | не баг | Учётная запись пункта выдачи топлива на стенде принадлежит организации 100002000, а испытательный парк — 025680800, поэтому выдача топлива через рабочее место пункта даёт 404 | Поведение верное (тенант-изоляция). Для проверки рабочего места нужна учётная запись пункта в той же организации | — |

---

## 4. Критерии готовности к продакшену

1. Все строки §2 — `[x]` с заполненной колонкой «как проверено».
2. Полные наборы тестов зелёные (кроме двух Testcontainers-тестов, не идущих на Windows).
3. `npx tsc --noEmit` без ошибок.
4. Логи контейнеров без ERROR после сквозного прогона всех ролей.
5. Каждое изменение: журнал + атомарный коммит + push в `origin/migration`.
6. Открытые «Вопросы к Максу» перечислены и не блокируют запуск (выбран безопасный вариант).
