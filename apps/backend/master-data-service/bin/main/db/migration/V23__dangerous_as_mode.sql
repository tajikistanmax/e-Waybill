-- «Опасные грузы» — больше НЕ отдельный тип путевого листа, а режим грузового ПЛ (2-Б):
-- отметка «опасный груз» + класс ADR + свидетельства ДОПОГ. Так корректнее (это характеристика
-- груза, а не форма). Деактивируем название в справочнике WAYBILL_TYPE (скрыт из выбора и из
-- редактируемого списка /settings/types). В enum WaybillType.WB_DANGEROUS остаётся для исторических
-- записей (отображается через зашитый дефолт WTYPE). Официальных форм — 7 (3-С/1-А/Т(1-АД)/…/4М-БМ).
update classifier set active = false where category = 'WAYBILL_TYPE' and code = 'WB_DANGEROUS';
