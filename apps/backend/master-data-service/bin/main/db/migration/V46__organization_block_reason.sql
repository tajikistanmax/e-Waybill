-- Блокировка организации (§УАТ-2026-09-04): раньше поле blocked менялось общим upsert
-- реквизитов без обязательной причины, аудит фиксировал только диф имени организации —
-- расследовать «кто и почему заблокировал» было невозможно. Причина теперь хранится на
-- самой организации (виден COMPANY_ADMIN без доступа к аудит-журналу) и дублируется
-- в audit_log через выделенные /block, /unblock эндпоинты (OrganizationController).
ALTER TABLE organization ADD COLUMN block_reason TEXT;
