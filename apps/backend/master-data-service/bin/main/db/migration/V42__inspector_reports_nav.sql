-- Инспектору дорожного контроля открыт раздел «Отчёты».
--
-- Бэкенд уже разрешает ему сводку за период и журналы предрейсового контроля
-- (ReportController: /summary, /journal/mechanic, /journal/doctor) — надзор должен
-- видеть, проводились ли осмотры на самом деле. Экономика перевозчика (топливо,
-- зарплата, разрезы) и сводные отчёты Минтранса ему по-прежнему закрыты — и на
-- бэкенде (@PreAuthorize), и в интерфейсе (вкладки фильтруются по роли).
--
-- Меню строится из этой таблицы, а не из зашитого lib/roles.ts: без этой строки
-- пункт «Отчёты» инспектору не показывался бы, хотя доступ к данным уже есть.

update role_access
set nav_keys = 'inspector,violations,monitoring,reports'
where role = 'INSPECTOR';
