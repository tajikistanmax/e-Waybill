-- Роль «Пункт выдачи топлива» в матрице доступа разделов.
--
-- Матрица role_access — источник меню и стартовой страницы (страница «Настройки → Роли»
-- показывает ровно те роли, что есть в таблице). Строки FUEL_STATION в ней не было, поэтому:
--   * интерфейс работал по запасному пути (зашитый lib/roles.ts даёт роли раздел «Топливо»),
--     то есть пользователь ничего не терял;
--   * но администратор НЕ мог настроить этой роли меню: её просто не было в списке на странице
--     «Роли» — 13 ролей из 14 (находка сквозной приёмки 22.09.2026, блок A3).
--
-- Значения совпадают с запасным путём в коде, поэтому поведение интерфейса не меняется:
-- роль как видела раздел «Топливо», так и видит, но теперь ею можно управлять.

insert into role_access (role, home_key, nav_keys) values
  ('FUEL_STATION', 'fuel', 'fuel')
on conflict (role) do nothing;
