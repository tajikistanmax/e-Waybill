-- Отметка «Копия» на печатном бланке (QA §17): счётчик печати ПЛ — первая печать выходит
-- без отметки, начиная со второй бланк подписывается «КОПИЯ», чтобы отличать пересъём
-- документа от оригинала, выданного водителю на руки.
ALTER TABLE waybill ADD COLUMN print_count INTEGER NOT NULL DEFAULT 0;
