import DictionariesView from '../DictionariesView';

/** Справочник городов и районов (регион, рамз, название) — как в админке старой платформы. */
export default function CitiesDictionaryPage() {
  return <DictionariesView tab="cities" />;
}
